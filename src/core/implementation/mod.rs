//! The submodules from this module provide an implementation for the core
//! abstractions of the library (those are described in `core::abstraction::*`).
pub mod mdd;
pub mod heuristics;
pub mod solver;